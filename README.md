# Wallpaper Toggle Script

A simple PowerShell script to toggle between two wallpapers on Windows 11.

## Usage

- Update the wallpaper paths in `config.ps1` to your desired images.
- Run the script by executing:

```powershell
powershell -ExecutionPolicy Bypass -File .\config.ps1
